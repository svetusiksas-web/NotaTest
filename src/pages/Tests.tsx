import{Copy,ExternalLink,Plus,Search,Trash2}from'lucide-react';import{useState}from'react';import{Link}from'react-router-dom';import{Header}from'../components/Header';import{demo}from'../data/demo';import{testService}from'../services/storage';import type{QuizTest}from'../types';
export function Tests(){const[,refresh]=useState(0);let tests=testService.all();if(!tests.length){testService.save(demo);tests=[demo]}const remove=(t:QuizTest)=>{if(confirm(`Удалить «${t.title}»? Результаты сохранятся.`)){testService.remove(t.id);refresh(x=>x+1)}};return <><Header eyebrow="Библиотека" title="Мои тесты"><Link className="button primary" to="/editor/new"><Plus/>Новый тест</Link></Header><div className="toolbar"><label><Search/><input placeholder="Поиск по названию…"/></label><select><option>Все статусы</option><option>Опубликован</option><option>Черновик</option></select></div><div className="test-grid">{tests.map(t=><article className="test-card" key={t.id}><div className="card-top"><span className={`status ${t.status}`}>{t.status==='published'?'Опубликован':'Черновик'}</span><span>{t.questions.length} вопросов</span></div><h3>{t.title}</h3><p>{t.subject} · {t.grade||'Все классы'}</p><div className="card-actions"><Link className="button" to={`/editor/${t.id}`}>Редактировать</Link>{t.status==='published'&&<Link title="Открыть" to={`/test/${t.slug}`}><ExternalLink/></Link>}<button title="Копировать ссылку" onClick={()=>navigator.clipboard.writeText(`${location.origin}/test/${t.slug}`)}><Copy/></button><button className="danger" title="Удалить" onClick={()=>remove(t)}><Trash2/></button></div></article>)}</div></>}
